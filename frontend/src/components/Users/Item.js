// import { useEffect, useRef, useState } from "react";
// import { useDispatch, useSelector } from "react-redux";
// import styles from "./Item.module.css";

// function Item({ itemId, currUser, onClose }) {
//   const dispatch = useDispatch();

//   // useEffect(() => {
//   //   (async () => {
//   //     try {
//   //       await dispatch(getUserById(followId));
//   //       setLoaded(true);
//   //     } catch (err) {}
//   //   })();
//   // }, [dispatch, followId]);

//   return (
//     <div className={styles.shopItemContainer}>
//       <img className={styles.itemImage} src={item.image} alt={item.image} />
//       {isCurrChoice && <button>Current</button>}
//     </div>
//   );
// }

// export default Item;
